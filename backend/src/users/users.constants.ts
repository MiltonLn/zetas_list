/**
 * Password assigned to accounts created by an admin or imported from the
 * WhatsApp group. Always paired with `mustChangePassword: true`.
 */
export const DEFAULT_PASSWORD = 'zetas123';

export const BCRYPT_ROUNDS = 12;
